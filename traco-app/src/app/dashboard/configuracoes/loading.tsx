import { PageSkeleton } from '@/components/shared/page-skeleton';

export default function ConfiguracoesLoading() {
  return (
    <PageSkeleton title="Configurações" subtitle="Personalize o que você oferece" blocks={3} />
  );
}
